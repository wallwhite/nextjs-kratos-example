export enum RegistrationFormField {
  FlowId = 'flow',
  CsrfToken = 'csrf_token',
  Email = 'email',
  FirstName = 'first',
  LastName = 'last',
  Password = 'password',
}

export enum LoginFormField {
  FlowId = 'flow',
  CsrfToken = 'csrf_token',
  Email = 'email',
  Password = 'password',
}
